/**
 * Authentication-related constants
 */

/**
 * JWT Token type
 */
export const TOKEN_TYPE = 'Bearer';

/**
 * Authorization scheme for Bearer tokens
 */
export const AUTH_SCHEME_BEARER = TOKEN_TYPE.toLowerCase();
